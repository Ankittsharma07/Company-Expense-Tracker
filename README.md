# Project Setup
    
    To run this project, follow these steps:
    
    1. Run `npm install` to install dependencies.
    2. Set `VITE_API_BASE_URL` (e.g. `http://localhost:4000`) in a `.env` file.
    3. Run `npm run dev` to start the development server.
    
